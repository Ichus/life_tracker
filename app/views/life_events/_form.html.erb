<%= form_for(@life_event) do |f| %>
  <% if @life_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@life_event.errors.count, "error") %> prohibited this life_event from being saved:</h2>

      <ul>
      <% @life_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row hug-left">
    <div class="small-2 columns"><%= f.label :person_id, "Name", class: "left inline" %></div>
    <div class="small-4 columns left hug-left"><%= f.collection_select :person_id, Person.all, :id, :name_joined %></div>
  </div>
  <div class="row hug-left">
    <div class="small-2 columns"><%= f.label :title, class: "left inline" %></div>
    <div class="small-4 columns left hug-left"><%= f.text_field :title %></div>
  </div>
  <div class="row hug-left">
    <div class="small-2 columns"><%= f.label :year, class: "left inline" %></div>
    <div class="small-4 columns left hug-left"><%= f.text_field :year %></div>
  </div>
  <div class="row hug-left">
    <div class="small-2 columns"><%= f.label :description, class: "left inline" %></div>
    <div class="small-10 columns left hug-left"><%= f.text_field :description %></div>
  </div>
  <div class="row">
    <div class="small-9 columns hug-left"><%= f.submit %></div>
  </div>
<% end %>
